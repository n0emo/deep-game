package game

World_Fight :: struct {}

world_fight_update :: proc(w: ^World) {}

world_fight_draw :: proc(w: ^World) {}

world_fight_ui :: proc(w: ^World, queue: ^Event_Queue) {}

world_fight_handle_event :: proc(w: ^World, event: Event) {}
